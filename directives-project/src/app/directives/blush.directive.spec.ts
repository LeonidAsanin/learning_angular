import { BlushDirective } from './blush.directive';

describe('BlushDirective', () => {
  it('should create an instance', () => {
    const directive = new BlushDirective();
    expect(directive).toBeTruthy();
  });
});
