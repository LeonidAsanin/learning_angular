import { DirectiveValidatorDirective } from './directive-validator.directive';

describe('DirectiveValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectiveValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
